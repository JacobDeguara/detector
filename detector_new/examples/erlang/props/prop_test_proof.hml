with
  system:start(_)
check
  [_ <- _, system:start(_)]
  rec X.(
    [_ ? {r}] ( [_:_ ! {s}]X )
    and
      (
        [_:_ ! {a}](X and [_ ? {d}]no)
          or
        [_:_ ! {c}]no
      )
  ).  